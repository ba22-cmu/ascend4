int ascjson::call_all_force_link() {
	const char *vargv = "-help";
	rcp e = NULL;
	int count = 0;
	e = Asc_LibrQueryTypeCmdHC (vargv);
	count += e->e;
	e = Asc_LibrOptionsCmdHC (vargv) ;
	count += e->e;
	e = Asc_LibrParseCmdHC (vargv) ;
	count += e->e;
	e = Asc_LibrReadCmdHC (vargv) ;
	count += e->e;
	e = Asc_LibrModuleInfoCmdHC (vargv) ;
	count += e->e;
	e = Asc_LibrDestroyTypesCmdHC (vargv) ;
	count += e->e;
	e = Asc_LibrHideTypeCmdHC (vargv) ;
	count += e->e;
	e = Asc_LibrUnHideTypeCmdHC (vargv) ;
	count += e->e;
	e = Asc_LibrTypeIsShownCmdHC (vargv) ;
	count += e->e;
	e = Asc_LibrTypeListCmdHC (vargv) ;
	count += e->e;
	e = Asc_ExtractTypeHC (vargv) ;
	count += e->e;
	e = qlfdid (vargv) ;
	count += e->e;
	e = bgetproc (vargv) ;
	count += e->e;
	e = Asc_BrowInitializeCmdHC (vargv) ;
	count += e->e;
	e = brow_assign (vargv) ;
	count += e->e;
	e = qassgn3 (vargv) ;
	count += e->e;
	e = x__brow_iname (vargv) ;
	count += e->e;
	e = x__brow_isrelation (vargv) ;
	count += e->e;
	e = x__brow_ismodel (vargv) ;
	count += e->e;
	e = bgetrels (vargv) ;
	count += e->e;
	e = bgetrelspf (vargv) ;
	count += e->e;
	e = x__brow_relsforatom (vargv) ;
	count += e->e;
	e = x__brow_islogrel (vargv) ;
	count += e->e;
	e = bgetlogrels (vargv) ;
	count += e->e;
	e = bgetlogrelspf (vargv) ;
	count += e->e;
	e = x__brow_logrelsforatom (vargv) ;
	count += e->e;
	e = bgetcondrels (vargv) ;
	count += e->e;
	e = bgetcondlogrels (vargv) ;
	count += e->e;
	e = x__brow_iswhen (vargv) ;
	count += e->e;
	e = x__brow_isinstanceinwhen (vargv) ;
	count += e->e;
	e = bgetwhens (vargv) ;
	count += e->e;
	e = x__brow_whensforinstance (vargv) ;
	count += e->e;
	e = bwritependings (vargv) ;
	count += e->e;
	e = bnumpendings (vargv) ;
	count += e->e;
	e = count_names (vargv) ;
	count += e->e;
	e = aliases (vargv) ;
	count += e->e;
	e = isas (vargv) ;
	count += e->e;
	e = cliques (vargv) ;
	count += e->e;
	e = Asc_BrowWriteInstanceCmdHC (vargv) ;
	count += e->e;
	e = bmerge (vargv) ;
	count += e->e;
	e = brefine (vargv) ;
	count += e->e;
	e = bmakealike (vargv) ;
	count += e->e;
	e = b_isplottable (vargv) ;
	count += e->e;
	e = b_prepplotfile (vargv) ;
	count += e->e;
	e = free_all_vars (vargv) ;
	count += e->e;
	e = Asc_SimBinTokenSetOptionsHC (vargv) ;
	count += e->e;
	e = sims (vargv) ;
	count += e->e;
	e = sim_instantiate (vargv) ;
	count += e->e;
	e = sim_unique (vargv) ;
	count += e->e;
	e = sim_reinstantiate (vargv) ;
	count += e->e;
	e = x__sims_copy (vargv) ;
	count += e->e;
	e = x__sims_proto (vargv) ;
	count += e->e;
	e = x__sims_saveinst (vargv) ;
	count += e->e;
	e = sim_destroy (vargv) ;
	count += e->e;
	e = simlistpending (vargv) ;
	count += e->e;
	e = ddefine (vargv) ;
	count += e->e;
	e = ddiffdefine (vargv) ;
	count += e->e;
	e = disp (vargv) ;
	count += e->e;
	e = hier (vargv) ;
	count += e->e;
	e = file_by_type (vargv) ;
	count += e->e;
	e = dchild (vargv) ;
	count += e->e;
	e = drefines_me (vargv) ;
	count += e->e;
	e = drefines_meall (vargv) ;
	count += e->e;
	e = drefinement_tree (vargv) ;
	count += e->e;
	e = dgetparts (vargv) ;
	count += e->e;
	e = disroot_type (vargv) ;
	count += e->e;
	e = Asc_ProbeCmdHC (vargv) ;
	count += e->e;
	e = x__var_analyze (vargv) ;
	count += e->e;
	e = x__rel_analyze (vargv) ;
	count += e->e;
	e = x__userdata_init (vargv) ;
	count += e->e;
	e = x__userdata_create (vargv) ;
	count += e->e;
	e = x__userdata_destroy (vargv) ;
	count += e->e;
	e = x__userdata_save (vargv) ;
	count += e->e;
	e = x__userdata_restore (vargv) ;
	count += e->e;
	e = x__userdata_query (vargv) ;
	count += e->e;
	e = x__userdata_print (vargv) ;
	count += e->e;
	e = get_model_children (vargv) ;
	count += e->e;
	e = slv_checksim (vargv) ;
	count += e->e;
	e = slv_checksys (vargv) ;
	count += e->e;
	e = slv_get_obj_list (vargv) ;
	count += e->e;
	e = slv_set_obj_by_num (vargv) ;
	count += e->e;
	e = Asc_SolvGetObjNumCmdHC (vargv) ;
	count += e->e;
	e = slv_get_parms (vargv) ;
	count += e->e;
	e = set_slv_parms (vargv) ;
	count += e->e;
	e = set_slv_parmsnew (vargv) ;
	count += e->e;
	e = slv_get_parmsnew (vargv) ;
	count += e->e;
	e = slv_get_insttype (vargv) ;
	count += e->e;
	e = slv_get_cost_page (vargv) ;
	count += e->e;
	e = slv_get_stat_page (vargv) ;
	count += e->e;
	e = slv_get_objval (vargv) ;
	count += e->e;
	e = slv_get_instname (vargv) ;
	count += e->e;
	e = slv_get_pathname (vargv) ;
	count += e->e;
	e = slv_get_vr (vargv) ;
	count += e->e;
	e = slvdump (vargv) ;
	count += e->e;
	e = slv_presolve (vargv) ;
	count += e->e;
	e = slv_reanalyze (vargv) ;
	count += e->e;
	e = slv_check_and_reanalyze (vargv) ;
	count += e->e;
	e = slv_set_independent (vargv) ;
	count += e->e;
	e = slv_resolve (vargv) ;
	count += e->e;
	e = slv_solve (vargv) ;
	count += e->e;
	e = slv_iterate (vargv) ;
	count += e->e;
	e = slv_available (vargv) ;
	count += e->e;
	e = slv_number (vargv) ;
	count += e->e;
	e = slv_name (vargv) ;
	count += e->e;
	e = slv_linsol_names (vargv) ;
	count += e->e;
	e = slv_eligible_solver (vargv) ;
	count += e->e;
	e = slv_select_solver (vargv) ;
	count += e->e;
	e = slv_get_solver (vargv) ;
	count += e->e;
	e = slv_flush_solver (vargv) ;
	count += e->e;
	e = slv_import_qlfdid (vargv) ;
	count += e->e;
	e = slv_lnmget (vargv) ;
	count += e->e;
	e = slv_lnmset (vargv) ;
	count += e->e;
	e = slv_set_haltflag (vargv) ;
	count += e->e;
	e = slvhelp (vargv) ;
	count += e->e;
	e = slv_near_bounds (vargv) ;
	count += e->e;
	e = slv_far_from_nominals (vargv) ;
	count += e->e;
	e = Asc_SolveMonitorCmdHC (vargv) ;
	count += e->e;
	e = dbg_get_blk_of_var (vargv) ;
	count += e->e;
	e = dbg_get_blk_of_eqn (vargv) ;
	count += e->e;
	e = dbg_get_blk_coords (vargv) ;
	count += e->e;
	e = dbg_get_eqn_of_var (vargv) ;
	count += e->e;
	e = dbg_get_varpartition (vargv) ;
	count += e->e;
	e = dbg_get_eqnpartition (vargv) ;
	count += e->e;
	e = dbg_list_rels (vargv) ;
	count += e->e;
	e = dbg_list_vars (vargv) ;
	count += e->e;
	e = dbg_write_var (vargv) ;
	count += e->e;
	e = dbg_write_unattvar (vargv) ;
	count += e->e;
	e = brow_write_var (vargv) ;
	count += e->e;
	e = dbg_write_rel (vargv) ;
	count += e->e;
	e = brow_write_rel (vargv) ;
	count += e->e;
	e = dbg_write_obj (vargv) ;
	count += e->e;
	e = brow_write_obj (vargv) ;
	count += e->e;
	e = dbg_write_varattr (vargv) ;
	count += e->e;
	e = dbg_write_qlfattr (vargv) ;
	count += e->e;
	e = dbg_rel_included (vargv) ;
	count += e->e;
	e = dbg_var_fixed (vargv) ;
	count += e->e;
	e = dbg_get_incidence (vargv) ;
	count += e->e;
	e = dbg_get_order (vargv) ;
	count += e->e;
	e = dbg_write_incidence (vargv) ;
	count += e->e;
	e = dbg_find_eligible (vargv) ;
	count += e->e;
	e = brow_find_eligible (vargv) ;
	count += e->e;
	e = dbg_consistency_analysis (vargv) ;
	count += e->e;
	e = dbg_global_eligible (vargv) ;
	count += e->e;
	e = dbg_find_activerels (vargv) ;
	count += e->e;
	e = brow_find_activerels (vargv) ;
	count += e->e;
	e = dbg_struct_singular (vargv) ;
	count += e->e;
	e = dbg_num_block_singular (vargv) ;
	count += e->e;
	e = var_free2nom (vargv) ;
	count += e->e;
	e = var_nom2free (vargv) ;
	count += e->e;
	e = dbg_calc_relnoms (vargv) ;
	count += e->e;
	e = dbg_check_rels (vargv) ;
	count += e->e;
	e = dbg_write_slv0_xsys (vargv) ;
	count += e->e;
	e = dbg_write_slv0_sys (vargv) ;
	count += e->e;
	e = dbg_mtxwriteplot (vargv) ;
	count += e->e;
	e = dbg_calc_jacobian (vargv) ;
	count += e->e;
	e = dbghelp (vargv) ;
	count += e->e;
	e = integrate_setup (vargv) ;
	count += e->e;
	e = integrate_cleanup (vargv) ;
	count += e->e;
	e = integrate_set_y_file (vargv) ;
	count += e->e;
	e = integrate_set_obs_file (vargv) ;
	count += e->e;
	e = integrate_logunits (vargv) ;
	count += e->e;
	e = integrate_logformat (vargv) ;
	count += e->e;
	e = integrate_get_samples (vargv) ;
	count += e->e;
	e = integrate_set_samples (vargv) ;
	count += e->e;
	e = integrate_able (vargv) ;
	count += e->e;
	e = x__mtx_norms (vargv) ;
	count += e->e;
	e = mtx_gui_plot_incidence (vargv) ;
	count += e->e;
	e = mtxhelp (vargv) ;
	count += e->e;
	e = u_destroy_units (vargv) ;
	count += e->e;
	e = u_setSIdef (vargv) ;
	count += e->e;
	e = u_getbasedef (vargv) ;
	count += e->e;
	e = u_dump (vargv) ;
	count += e->e;
	e = u_dims (vargv) ;
	count += e->e;
	e = u_dim_setverify (vargv) ;
	count += e->e;
	e = u_dim2num (vargv) ;
	count += e->e;
	e = u_num2dim (vargv) ;
	count += e->e;
	e = u_frombasedim (vargv) ;
	count += e->e;
	e = u_fromatomdim (vargv) ;
	count += e->e;
	e = u_getdimatoms (vargv) ;
	count += e->e;
	e = u_change_baseunit (vargv) ;
	count += e->e;
	e = u_getprec (vargv) ;
	count += e->e;
	e = u_setprec (vargv) ;
	count += e->e;
	e = u_get_atoms (vargv) ;
	count += e->e;
	e = u_get_units (vargv) ;
	count += e->e;
	e = u_set_user (vargv) ;
	count += e->e;
	e = u_get_user (vargv) ;
	count += e->e;
	e = u_get_list (vargv) ;
	count += e->e;
	e = u_clear_user (vargv) ;
	count += e->e;
	e = u_getval (vargv) ;
	count += e->e;
	e = u_browgetval (vargv) ;
	count += e->e;
	e = u_slvgetrelval (vargv) ;
	count += e->e;
	e = u_slvgetvarval (vargv) ;
	count += e->e;
	e = u_slvgetobjval (vargv) ;
	count += e->e;
	e = uhelp (vargv) ;
	count += e->e;
	e = srefine (vargv) ;
	count += e->e;
	e = smerge (vargv) ;
	count += e->e;
	// e = ascloadwin (vargv) ;
	// count += e->e;
	e = gnutext (vargv) ;
	count += e->e;
	e = Asc_HelpCmdHC (vargv);
	count += e->e;
	return count;
}
